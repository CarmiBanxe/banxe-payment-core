# Hyperswitch full stack — Sprint 9
# USE: git clone https://github.com/juspay/hyperswitch on GMKtec
# Then apply port remaps below:
#   sed -i 's/"5432:5432"/"5434:5432"/g' docker-compose.yml
#   sed -i 's/"8080:8080"/"8096:8080"/g' docker-compose.yml  
#   sed -i 's/"6379:6379"/"6380:6379"/g' docker-compose.yml
#   sed -i 's/"9000:8080"/"8097:8080"/g' docker-compose.yml
#   docker compose up -d
#
# Ports (ADR-015 / SERVICE-MAP):
#   App Server:       8096
#   Control Center:   8097
#   Web SDK:          9050
#   PG (internal):    5434
#   Redis (internal): 6380
#
# NOTE: Official juspay/hyperswitch docker-compose is used directly.
# This file documents port remapping only.
